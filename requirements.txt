# Core Raspberry Pi GPIO
RPi.GPIO>=0.7.1

# Speech-to-text
SpeechRecognition>=3.14.0
pyaudio>=0.2.14

# Transformers + ML (Light enough for Pi; works with Python 3.11)
transformers==4.40.0
accelerate>=0.25.0
torch==2.1.0

# Utility Libraries
numpy>=1.24.0
pillow>=10.0.0

# Networking
socket

# Optional: Camera usage
opencv-python>=4.8.0

# For wake word detection (optional improvement)
openwakeword>=0.6.0
